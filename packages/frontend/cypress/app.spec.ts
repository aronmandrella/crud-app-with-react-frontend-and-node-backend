it("App E2E test", () => {
  /*
    NOTE:
    Cypress tests could be added here.
    They could use a test server instead of mocking API calls
    or do a very similar thing to tests in "src/components/App/App.spec.tsx"
  */

  expect(true).toBe(true);
});

export {};
